# Server Configuration
PORT=4001

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here

# PostgreSQL Database Configuration (Required for PostgreSQL setup)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=logistics_db
DB_USER=postgres
DB_PASS=your_postgres_password

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Optional: DATABASE_URL for cloud deployments (Render, Heroku, etc.)
# DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Instructions:
# 1. Copy this file to .env
# 2. Update DB_PASS with your actual PostgreSQL password
# 3. Ensure PostgreSQL is running on your system
# 4. Create a database named 'logistics_db' in PostgreSQL
